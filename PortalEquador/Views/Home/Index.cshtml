@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
   

    @*     
    @if (User.IsInRole(Roles.Administrator))
    {
    *@

    <h1 class="display-4">Gestão</h1>
    <br />
    <br />

        <div class="row">
        <div class="col-sm-6">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title"><i class="fa-solid fa-database"></i> Grupos e tipos</h4>
                    <p class="card-text">Gestão de  grupos e caracterização de descrições</p>
                    <div class="d-grid gap-2 col-6 mx-auto">
                        <a asp-controller="Groups" asp-action="Index" class="btn btn-primary">Gerir</a>
                    </div>
                </div>
            </div>
        </div>
            <div class="col-sm-6">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Administradores</h5>
                        <p class="card-text">With supporting text below as a natural lead-in to additional content.</p>
                        <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-primary">Gerir Administradores</a>
                    </div>
                </div>
            </div>
        </div>

    @*     } *@

</div>
